<Window x:Class="Fulbank.Views.OperationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fulbank.Views"
        mc:Ignorable="d"
        Title="OperationView" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Operations}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdOperation" Binding="{Binding IdOperation}" Width="*"/>
                <DataGridTextColumn Header="DateOperation" Binding="{Binding DateOperation}" Width="*"/>
                <DataGridTextColumn Header="IpDestinataire" Binding="{Binding IpDestinataire}" Width="*"/>
                <DataGridTextColumn Header="IpSource" Binding="{Binding IpSource}" Width="*"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="*"/>
                <DataGridTextColumn Header="IdCryptoWallet" Binding="{Binding IdCryptoWallet}" Width="*"/>
                <DataGridTextColumn Header="IdCryptoWallet_1" Binding="{Binding IdCryptoWallet_1}" Width="*"/>
                <DataGridTextColumn Header="IdCompte" Binding="{Binding IdCompte}" Width="*"/>
                <DataGridTextColumn Header="IdCompte_1" Binding="{Binding IdCompte_1}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
