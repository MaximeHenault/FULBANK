<Window x:Class="Fulbank.Views.PossedeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fulbank.Views"
        mc:Ignorable="d"
        Title="PossedeView" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Possedes}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdUtilisateur" Binding="{Binding IdUtilisateur}" Width="*"/>
                <DataGridTextColumn Header="IdCompte" Binding="{Binding IdCompte}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
