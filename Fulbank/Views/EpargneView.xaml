<Window x:Class="Fulbank.Views.EpargneView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fulbank.Views"
        mc:Ignorable="d"
        Title="EpargneView" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Epargnes}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdEpargne" Binding="{Binding IdCompte}" Width="100"/>
                <DataGridTextColumn Header="Taux" Binding="{Binding Taux}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
