<Window x:Class="Fulbank.Views.WalletView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fulbank.Views"
        mc:Ignorable="d"
        Title="WalletView" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Wallets}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdWallet" Binding="{Binding IdWallet}" Width="*"/>
                <DataGridTextColumn Header="IdCryptoWallet" Binding="{Binding IdCryptoWallet}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
